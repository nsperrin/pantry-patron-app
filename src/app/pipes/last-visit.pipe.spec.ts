import { LastVisitPipe } from './last-visit.pipe';

describe('LastVisitPipe', () => {
  it('create an instance', () => {
    const pipe = new LastVisitPipe();
    expect(pipe).toBeTruthy();
  });
});
